# Environment Variables Template
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# Application
VITE_APP_NAME=FactorBeam
VITE_APP_ENV=development

# API Configuration (when backend is added)
# VITE_API_URL=https://api.factorbeam.com
# VITE_API_TIMEOUT=30000

# Security
# VITE_ENABLE_RATE_LIMITING=true
# VITE_MAX_REQUEST_SIZE=10485760

# Analytics (when implemented)
# VITE_ANALYTICS_ID=
# VITE_ANALYTICS_ENABLED=false

# Feature Flags
# VITE_ENABLE_GAMES=true
# VITE_ENABLE_ASSESSMENTS=true

# IMPORTANT SECURITY NOTES:
# 1. Never store sensitive API keys in environment variables exposed to the client
# 2. Only use VITE_ prefix for variables that can be safely exposed to the browser
# 3. Server-side secrets should be stored in backend environment only
# 4. Use different values for development, staging, and production
# 5. Rotate secrets regularly
# 6. Use a secrets management service in production (AWS Secrets Manager, HashiCorp Vault, etc.)